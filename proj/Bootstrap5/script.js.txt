const navLinks =
document.querySelectorAll(".nav-link");

navLinks.forEach(link => {

    link.addEventListener("click", () => {

        navLinks.forEach(nav =>
            nav.classList.remove("active"));

        link.classList.add("active");

    });

});

const joinButtons =
document.querySelectorAll(".btn-primary, .btn-success, .btn-danger");

joinButtons.forEach(button => {

    button.addEventListener("click", () => {

        alert("Successfully Registered for Event!");

    });

});
const cards =
document.querySelectorAll(".card");

window.addEventListener("scroll", () => {

    cards.forEach(card => {

        const cardTop =
        card.getBoundingClientRect().top;

        const screenHeight =
        window.innerHeight;

        if(cardTop < screenHeight - 100){

            card.classList.add("show-card");

        }

    });

});

const footer =
document.querySelector("footer p");

const year =
new Date().getFullYear();

footer.innerHTML =
`© ${year} NeoFest | All Rights Reserved`;

window.addEventListener("load", () => {

    document.body.classList.add("loaded");

});

console.log(

    "NeoFest 2026 Bootstrap Project Loaded Successfully"

);


const typingText =
document.querySelector(".typing-text");

const words = [

    "AI • Robotics • Gaming • Startups",

    "Future Innovation Summit",

    "Cyber Security • Web3 • Metaverse"

];

let wordIndex = 0;

setInterval(() => {

    typingText.textContent = words[wordIndex];

    wordIndex++;

    if(wordIndex >= words.length){

        wordIndex = 0;

    }

},2500);